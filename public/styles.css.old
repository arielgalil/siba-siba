/* הגדרות בסיסיות עבור מצב בהיר */
body, html {
  margin: 0;
  padding: 0;
  height: 100%;
  background-color: #f8f9fa;
  color: #000;
}

.container {
  min-height: 100vh;
  padding: 20px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20px;
  background-color: transparent;
}

/* עיצוב תיבות בסיסי */
.text-box {
  padding: 10px 40px 10px 15px;
  border: 1px solid #ced4da;
  border-radius: 20px;
  background-color: #ffffff;
  color: #000000;
  font-size: 16px;
  cursor: grab;
  position: relative;
  transition: background-color 0.3s, color 0.3s;
}

.text-box.correct {
  background-color: #d4edda;
  border-color: #c3e6cb;
  color: #155724;
  cursor: default;
}

.text-box.wrong {
  background-color: #f8d7da;
  border-color: #f5c6cb;
  color: #721c24;
}

.text-box.fixed {
  cursor: default;
}

/* עיצוב האייקונים */
.icon {
  font-size: 20px;
  color: #6c757d;
}

/* עיצוב כפתור הבדיקה */
.check-button {
  background-color: #28a745;
  color: white;
  padding: 10px 20px;
  border: none;
  border-radius: 20px;
  font-size: 18px;
  cursor: pointer;
}

.check-button.ready {
  background-color: #ffc107;
  color: #000;
}

.check-button:disabled {
  background-color: #6c757d;
  cursor: not-allowed;
}

/* אזור הודעות – גובה קבוע */
.message {
  min-height: 2rem;
  font-size: 1rem;
  text-align: center;
}

/* איחוד גופנים בכל הממשק, למעט הכותרת */
body *:not(.title) {
  font-size: 16px;
}

/* אנימציית פעימה */
@keyframes pulseAnim {
  0% { transform: scale(1); }
  50% { transform: scale(1.2); }
  100% { transform: scale(1); }
}
.pulse {
  animation: pulseAnim 1s ease;
}

/* הגדרות מצב כהה */
@media (prefers-color-scheme: dark) {
  body, html {
    background-color: #1a202c;
    color: #fff;
  }
  
  .container {
    background-color: transparent;
  }
  
  .text-box {
    background-color: #2d3748;
    border-color: #4a5568;
    color: #fff;
  }
  
  .text-box.correct {
    background-color: #2f855a;
    border-color: #276749;
    color: #d4edda;
  }
  
  .text-box.wrong {
    background-color: #c53030;
    border-color: #9b2c2c;
    color: #f8d7da;
  }
  
  .icon {
    color: #a0aec0;
  }
  
  .check-button {
    background-color: #48bb78;
    color: #fff;
  }
  
  .check-button.ready {
    background-color: #ecc94b;
    color: #000;
  }
  
  body {
    background-color: #1a202c !important;
  }
}

/* תיבת משפט - תוכן פנימי עם אייקון וטקסט */
.text-box > div {
  display: flex;
  align-items: center;
  gap: 0.5rem; /* רווח קבוע בין האייקון לטקסט */
}

.no-select {
  -webkit-user-select: none; /* Safari */
  -moz-user-select: none; /* Firefox */
  -ms-user-select: none; /* IE/Edge */
  user-select: none; /* Standard */
}